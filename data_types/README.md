| Idx | Data / Action                         | Main APIs (latest Android)                                                                                          | Access level (latest)                                                                                                                                                                     | Notes (if earlier requirements differs)                                                                                                                                                         |
|-----|---------------------------------------|----------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | Call phone                            | `Intent.ACTION_CALL`, `TelecomManager#placeCall()`                                                                  | `CALL_PHONE` – **dangerous** (runtime). `ACTION_DIAL` needs no permission.                                                                                                                | Model basically unchanged historically.                                                                                                                                            |
| 2   | Carrier info                          | `TelephonyManager`, `SubscriptionManager`, `CarrierConfigManager`                                                   | Many detailed fields require `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**.                                                                                                      | Earlier: more carrier fields accessible via `READ_PHONE_STATE` (**dangerous**).                                                                                                   |
| 3   | Cellbroadcast                         | System cell-broadcast / emergency app (no stable public API)                                                        | Effectively **system/privileged only**.                                                                                                                                                   | Earlier: some CB/SMS broadcasts were visible with `RECEIVE_SMS` (**dangerous**).                                                                                                  |
| 4   | External storage                      | `MediaStore`, Storage Access Framework (`ACTION_OPEN_DOCUMENT`), `MANAGE_EXTERNAL_STORAGE`                          | `READ_MEDIA_IMAGES/VIDEO/AUDIO` – **dangerous** for media; `MANAGE_EXTERNAL_STORAGE` – **special all-files access**.                                                                     | Earlier: broad access via `READ_EXTERNAL_STORAGE` / `WRITE_EXTERNAL_STORAGE` (**dangerous**).                                                                                    |
| 5   | ICCID                                 | `SubscriptionInfo#getIccId()` via `SubscriptionManager`                                                             | `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**.                                                                                                                                    | Earlier: accessible via `READ_PHONE_STATE` (**dangerous**).                                                                                                                       |
| 6   | IMEI                                  | `TelephonyManager#getImei()` / `getDeviceId()`                                                                      | `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**.                                                                                                                                    | Earlier: exposed via `READ_PHONE_STATE` (**dangerous**).                                                                                                                          |
| 7   | IMSI                                  | `TelephonyManager#getSubscriberId()`                                                                                | `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**.                                                                                                                                    | Earlier: exposed via `READ_PHONE_STATE` (**dangerous**).                                                                                                                          |
| 8   | MEID                                  | `TelephonyManager#getMeid()`                                                                                        | `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**.                                                                                                                                    | Earlier: exposed via `READ_PHONE_STATE` (**dangerous**).                                                                                                                          |
| 9   | Network info                          | `ConnectivityManager#getActiveNetwork()`, `getNetworkCapabilities()`                                                | `ACCESS_NETWORK_STATE` – **normal**.                                                                                                                                                      | Similar historically.                                                                                                                                                              |
| 10  | Phone status                          | `TelephonyManager#getCallState()`, `getServiceState()`, etc.                                                        | Many fields need `READ_PHONE_STATE` and/or `READ_PRIVILEGED_PHONE_STATE` – often **system/privileged**.                                                                                   | Earlier: mostly reachable with `READ_PHONE_STATE` (**dangerous**).                                                                                                                |
| 11  | SD card                               | Same as external: `MediaStore`, SAF, app-specific external dirs                                                     | Own app dirs: no special perm; shared media: `READ_MEDIA_*` (**dangerous**); all-files: `MANAGE_EXTERNAL_STORAGE` (**special**).                                                         | Earlier: SD card broadly accessible via `READ_EXTERNAL_STORAGE` / `WRITE_EXTERNAL_STORAGE` (**dangerous**).                                                                      |
| 12  | serial (device serial)                | `Build.getSerial()`                                                                                                 | `READ_PRIVILEGED_PHONE_STATE` – **system/privileged** (3rd-party usually sees `"UNKNOWN"`).                                                                                              | Earlier: `Build.SERIAL` was readable (sometimes with `READ_PHONE_STATE` **dangerous**, or none on very old versions).                                                            |
| 13  | SIM info                              | `TelephonyManager` (`getSimState()`, `getSimOperator()`, etc.), `SubscriptionManager`                               | Identifying details: `READ_PRIVILEGED_PHONE_STATE` – **system/privileged**; some coarse fields (e.g. country) may still be public.                                                      | Earlier: many SIM details via `READ_PHONE_STATE` (**dangerous**).                                                                                                                |
| 14  | read SMS                              | `ContentResolver` on `Telephony.Sms.*` URIs                                                                         | `READ_SMS` – **dangerous**; many write/delete ops restricted to default SMS app.                                                                                                         | Same base permission; default-SMS role restrictions tightened over time.                                                                                                          |
| 15  | Telephone number                      | `TelephonyManager#getLine1Number()`, `SubscriptionInfo#getNumber()`                                                 | `READ_PHONE_NUMBERS` – **dangerous** (and sometimes `READ_PHONE_STATE`).                                                                                                                 | Earlier: usually under `READ_PHONE_STATE` (**dangerous**).                                                                                                                        |
| 16  | Camera                                | `android.hardware.camera2` (Camera2), CameraX                                                                       | `CAMERA` – **dangerous**.                                                                                                                                                                 | Same historically.                                                                                                                                                                |
| 17  | Location (coarse/fine)                | `LocationManager`, `FusedLocationProviderClient`                                                                    | `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` – **dangerous**; `ACCESS_BACKGROUND_LOCATION` – **dangerous** for background.                                                          | Earlier: background vs foreground rules were looser, no dedicated background perm.                                                                                                |
| 18  | Android ID                            | `Settings.Secure.getString(..., ANDROID_ID)`                                                                       | No dangerous permission; readable by the app itself. Scoped per signing key / user.                                                                                                      | Earlier: behaved like a global cross-app device ID.                                                                                                                               |
| 19  | App list                              | `PackageManager#getInstalledPackages()`, `getInstalledApplications()`                                               | To see all: `QUERY_ALL_PACKAGES` – **normal** but **policy-restricted**; otherwise only a filtered subset is visible.                                                                    | Earlier: all installed apps visible without `QUERY_ALL_PACKAGES`.                                                                                                                |
| 20  | Audio record                          | `MediaRecorder`, `AudioRecord`                                                                                      | `RECORD_AUDIO` – **dangerous**.                                                                                                                                                           | Same historically.                                                                                                                                                                |
| 21  | Google Ad ID                          | Play services `AdvertisingIdClient.getAdvertisingIdInfo()`                                                          | `com.google.android.gms.permission.AD_ID` – **normal**; further limited by user settings & Play policy.                                                                                  | Earlier: accessed via similar Play-services API with looser explicit permission expression.                                                                                      |
| 22  | The `media` directory                 | App external media dir (`Android/media/<pkg>`), via `getExternalMediaDirs()` / `getExternalFilesDir()`             | For **your own** `Android/media` dir: no extra storage permission; other apps’ media follow external rules (`READ_MEDIA_*` **dangerous** or `MANAGE_EXTERNAL_STORAGE` **special**).      | Earlier: app-specific vs general external separation less strict.                                                                                                                |
| 23  | OAID                                  | Vendor OAID SDK APIs (Huawei, Xiaomi, etc.)                                                                         | Depends on OEM; usually no standard Android **dangerous** perm; may require vendor whitelisting / signature.                                                                             | Always vendor-specific, outside core Android framework.                                                                                                                          |
| 24  | Screen record (screenshot or video)   | `MediaProjectionManager#createScreenCaptureIntent()` → `MediaProjection` + `VirtualDisplay` + `MediaRecorder`/`ImageReader` | **No manifest dangerous perm** for screen capture; requires **user-confirmation system dialog** each session. For mic audio: `RECORD_AUDIO` – **dangerous**. System screenshot APIs are **system/privileged**. | MediaProjection model exists since Lollipop; constraints have increased.                                                                                                         |
| 25  | Bluetooth connection                  | `BluetoothAdapter`, `BluetoothDevice`, `BluetoothGatt`, `BluetoothManager`                                          | Android 12+: `BLUETOOTH_CONNECT` – **dangerous**; typically also `BLUETOOTH_SCAN` – **dangerous** for discovery.                                                                          | Earlier: `BLUETOOTH` / `BLUETOOTH_ADMIN` (**normal**) + `ACCESS_FINE_LOCATION` (**dangerous**) for BLE scanning.                                                                  |
| 26  | Bluetooth call                        | `TelecomManager`, `BluetoothHeadset`, `BluetoothProfile`                                                            | `CALL_PHONE` – **dangerous** to place calls; `BLUETOOTH_CONNECT` – **dangerous** for headset interaction. Many full telephony APIs are **system/default-dialer only**.                   | Earlier: third-party dialers could do more with the same core permissions.                                                                                                       |
| 27  | Bluetooth ID                          | `BluetoothDevice#getName()`, `getAddress()`, BLE IDs in `ScanResult`                                               | `BLUETOOTH_CONNECT` / `BLUETOOTH_SCAN` – **dangerous**; scanning treated similar to location.                                                                                            | Earlier: addresses/names exposed more liberally with `BLUETOOTH`/`BLUETOOTH_ADMIN` and location perm.                                                                            |
| 28  | Bluetooth MAC (local)                 | `BluetoothAdapter#getAddress()`                                                                                     | Real local MAC is **system/privileged only**; 3rd-party typically sees a placeholder/constant.                                                                                           | Earlier: real MAC accessible with `BLUETOOTH` (**normal**).                                                                                                                      |
| 29  | IP address                            | `NetworkInterface`, `InetAddress`, `ConnectivityManager#getLinkProperties()`                                       | Usually no dangerous perm; sometimes `ACCESS_NETWORK_STATE` – **normal**. Public IP usually obtained via a remote service, not a special permission.                                     | Similar historically.                                                                                                                                                             |
| 30  | SIP service (VOIP)                    | `android.net.sip.SipManager` (deprecated)                                                                          | `USE_SIP` – **dangerous**; `INTERNET` – **normal**; for audio: `RECORD_AUDIO` – **dangerous**.                                                                                           | Same core permissions historically, but APIs are deprecated.                                                                                                                     |
| 31  | SSID/BSSID                            | `WifiManager#getConnectionInfo()` → `WifiInfo#getSSID()`, `getBSSID()`                                             | `ACCESS_FINE_LOCATION` – **dangerous**; `ACCESS_WIFI_STATE` – **normal**; Wi-Fi scans may also require `NEARBY_WIFI_DEVICES` – **dangerous**.                                            | Earlier: SSID/BSSID often accessible without location permission.                                                                                                                |
| 32  | Ultra-wideband                        | `UwbManager`, `UwbSession` (on devices that support UWB)                                                           | `UWB_RANGING` – **dangerous** (nearby / location-like).                                                                                                                                   | No framework UWB API on older versions.                                                                                                                                           |
| 33  | Connect Wifi                          | `WifiNetworkSpecifier`, `WifiNetworkSuggestion`, `CHANGE_WIFI_STATE`                                               | `NEARBY_WIFI_DEVICES` – **dangerous** (Android 13+), `CHANGE_WIFI_STATE` – **normal**; user confirmation is common for actual connections.                                               | Earlier: apps could configure/join networks more directly with `CHANGE_WIFI_STATE` alone.                                                                                        |
| 34  | Wifi MAC                              | `WifiInfo#getMacAddress()`                                                                                         | Real hardware MAC is **system/privileged only**; 3rd-party apps see randomized or placeholder MACs.                                                                                      | Earlier: real MAC accessible with `ACCESS_WIFI_STATE` (**normal**).                                                                                                             |
| 35  | Account info                          | `AccountManager#getAccounts()`, `getAccountsByType()`, account picker intents                                      | Raw enumeration heavily restricted; `GET_ACCOUNTS` is conceptually **dangerous**, but on modern Android many flows rely on **user-chosen account** dialogs instead of free listing.      | Earlier: `GET_ACCOUNTS` (**dangerous**) could list all accounts.                                                                                                                 |
| 36  | Browser bookmarks                     | No stable public API for modern browsers; data is app-private                                                      | Effectively **no access** for third-party apps; only browser/system can read.                                                                                                            | Earlier: legacy `Browser` provider with `READ_HISTORY_BOOKMARKS` (**dangerous**) exposed bookmarks/history cross-app.                                                            |
| 37  | Calendar                              | `CalendarContract` via `ContentResolver`                                                                           | `READ_CALENDAR`, `WRITE_CALENDAR` – both **dangerous**.                                                                                                                                  | Same core permissions historically.                                                                                                                                               |
| 38  | Clipboard data                        | `ClipboardManager#getPrimaryClip()`                                                                                | No manifest dangerous perm, but: only foreground apps (and IME/system) can read; access shows system toast/notification on modern Android. Background polling is blocked by app-ops.    | Earlier: background apps could often poll clipboard more freely.                                                                                                                 |
| 39  | Contact list                          | `ContactsContract` (`Contacts`, `Data`, `Phone`, etc.)                                                             | `READ_CONTACTS`, `WRITE_CONTACTS` – both **dangerous**.                                                                                                                                  | Model largely unchanged.                                                                                                                                                          |
| 40  | Contact log (call log)                | `CallLog.Calls` via `ContentResolver`                                                                              | `READ_CALL_LOG` – **dangerous**; `WRITE_CALL_LOG` – **dangerous**, often restricted to default dialer/system.                                                                            | Earlier: similar, but dialer-role restrictions were weaker.                                                                                                                     |